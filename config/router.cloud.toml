[router]
backoff_base_seconds = 0.25
backoff_max_seconds = 2.0
health_failure_threshold = 3
health_cooldown_seconds = 30.0

# The public beta uses OpenRouter's zero-cost model router. The application
# cannot select a paid model, and our own daily quota stays within the account's
# published free allowance. Add other approved providers later through the same
# adapter interface when their official keys are available.
[[providers]]
id = "openrouter_free"
adapter = "openai_compatible"
base_url = "https://openrouter.ai/api/v1"
api_key_env = "OPENROUTER_API_KEY"
enabled = true
timeout_seconds = 50.0
extra_headers = { "HTTP-Referer" = "https://modernization-ai-router.vercel.app", "X-Title" = "Modernization AI Router" }

[[models]]
id = "openrouter-free-auto"
provider_id = "openrouter_free"
upstream_model = "openrouter/free"
capabilities = ["code_analysis", "refactoring", "test_generation", "architecture", "debugging", "knowledge_answering"]
cost_tier = "free"
priority = 10
approved_for_proprietary = false
requests_per_minute = 10
requests_per_day = 50
tokens_per_minute = 60000
tokens_per_day = 300000
input_cost_per_million = 0.0
output_cost_per_million = 0.0
max_retries = 1
timeout_seconds = 50.0
